import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// OpenNext の Cloudflare アダプター設定。
// 必要になったら incrementalCache(R2)などのオーバーライドをここに追加する。
// 公式ドキュメント: https://opennext.js.org/cloudflare/get-started
export default defineCloudflareConfig({});
